<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Contacts.Views.AddEditProfileView" 
             Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="save.png" Command="{Binding OpenSaveCommand}"/>
    </ContentPage.ToolbarItems>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="{Binding Image}" Margin="0,30,0,0">
            <Image.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ImageCommand}"/>
            </Image.GestureRecognizers>
        </Image>
        <StackLayout Grid.Row="1" Padding="30,10">
            <Entry Placeholder="Nickname" Text="{Binding Nickname}"/>
            <Entry Placeholder="Name" Text="{Binding Name}"/>
        </StackLayout>
        <Editor Grid.Row="2" HeightRequest="170" MaxLength="120" Placeholder="Description" BackgroundColor="LightGray" Margin="30,0,30,30" Text="{Binding Description}"/>
    </Grid>
</ContentPage>    